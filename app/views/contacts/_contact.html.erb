<table>
  <thead>
    <th>Name</th>
    <th>Date of birth</th>
    <th>Phone</th>
    <th>Address</th>
    <th>Credit card number</th>
    <th>Credit card last four digits</th>
    <th>Credit card network</th>
    <th>Email</th>
    <th>Imported at</th>
  </thead>
  <tbody>
    <tr>
      <td><%= contact.name %></td>
      <td><%= contact.date_of_birth %></td>
      <td><%= contact.phone %></td>
      <td><%= contact.address %></td>
      <td><%= contact.credit_card_number %></td>
      <td><%= contact.credit_card_last_four_digits %></td>
      <td><%= contact.credit_card_network %></td>
      <td><%= contact.email %></td>
      <td><%= contact.created_at %></td>
    </tr>
  </tbody>
<table>